import { PluginBase } from "../../core/index.js";
import { JSX, ReactElement } from "react";
/**
 * Represents a pause interval within the playback, defined as a tuple of start and end timestamps (in seconds).
 */
export type PauseInterval = [number, number];
/**
 * Contextual information provided to playback plugins during visualization.
 * - `filePath`: Path to the file being visualized.
 * - `captureStartTimestamp`: First timestamp of the original capture (in seconds).
 * - `fileCaptureStartTimestamp`: Timestamp of the first frame in the file (in seconds).
 * - `pauseIntervals`: Intervals when playback is paused (in seconds).
 */
export interface PlaybackContext {
    filePath: string;
    captureStartTimestamp: number;
    fileCaptureStartTimestamp: number;
    pauseIntervals: PauseInterval[];
}
/**
 * Playback controls for interacting with session playback in the app.
 *
 * These controls allow the plugin's view to subscribe to playback events (play, pause, seek, sync)
 * triggered by the user or by the application UI. Each method allows the plugin to react to user interactions
 * and stay in sync with the playback state managed by the app.
 *
 * All callbacks return an unsubscribe function, which should be called to remove the listener when the plugin
 * is unmounted or no longer needs to listen for that event.
 */
export interface PlaybackControls {
    /**
     * Registers a callback for the play event.
     * Called whenever playback starts or resumes, providing the current time in milliseconds.
     * Useful for starting rendering, animations, or other time-dependent processes within the plugin.
     * @param callback - Function to invoke on play (receives current playback time in ms).
     * @returns Unsubscribe function.
     */
    onPlay: (callback: (fromTimeMs: number) => void) => () => void;
    /**
     * Registers a callback for the pause event.
     * Called whenever playback is paused by the user or the app.
     * Useful for pausing animations, stopping timers, or handling plugin-specific UI changes.
     * @param callback - Function to invoke on pause.
     * @returns Unsubscribe function.
     */
    onPause: (callback: () => void) => () => void;
    /**
     * Registers a callback for the seek event.
     * Called whenever playback is moved to a new position in time (by user or app).
     * Useful for jumping to a different point in the recording or visualization.
     * @param callback - Function to invoke on seek (receives the new playback time in ms).
     * @returns Unsubscribe function.
     */
    onSeek: (callback: (toTimeMs: number) => void) => () => void;
    /**
     * Registers a callback for the sync event.
     * Called regularly (e.g., in a loop during playback) to synchronize the current playback time with the plugin,
     * ensuring consistent rendering and state.
     * Useful for updating the internal state of the plugin based on the current playback time.
     * @param callback - Function to invoke on sync (receives the current playback time in ms).
     * @returns Unsubscribe function.
     */
    onSync: (callback: (currentTimeMs: number) => void) => () => void;
}
/**
 * Props passed to a playback plugin's view, including playback controls, context, and plugin settings.
 */
export interface PlaybackViewProps {
    controls: PlaybackControls;
    context: PlaybackContext;
    settings: Record<string, unknown>;
}
/**
 * Abstract base class for playback plugins.
 *
 * Playback plugins are used to visualize previously captured multimodal sessions in the app.
 *
 * Each plugin provides:
 * - A **preview** for display on the main (home) page, using `getPreview`. This is a static or dynamic representation
 *   (e.g., miniature graph, waveform, or visualization) that gives the user an idea of what the plugin provides.
 *   The preview is rendered based on configurations created for the plugin and is visible before a session is selected for playback.
 *
 * - A **main view** for interactive session playback, using `getView`. This is rendered when the user navigates to a specific session for a project and participant,
 *   and is where full playback occurs (with real data). The view can respond to play, pause, seek, and sync events through the provided controls.
 *
 * Attributes:
 * - `context`: The playback context, including file path and timestamps, injected by the application.
 *
 * Required methods:
 * - `getView(props)`: Renders the main playback UI for visualizing the session (with interactive controls).
 * - `getPreview()`: Renders a preview visualization for the home page or configuration selection.
 * - `validExtensions()`: Returns a list of supported file extensions (e.g., ["mp4", "json"]) for captured session files.
 */
export declare abstract class PlaybackPlugin extends PluginBase {
    protected context: PlaybackContext;
    static readonly __module: string;
    /**
     * Returns the main visualization view for the plugin.
     *
     * Called when a user opens a captured session for a specific project and participant.
     * This is the primary, interactive UI for session playback. The view should use the provided controls
     * (play, pause, seek, sync) to update and react to playback events from the host app.
     *
     * @param props - Contains playback controls, context (file/timestamps), and plugin settings.
     * @returns The plugin's visualization React element.
     */
    abstract getView(props: PlaybackViewProps): JSX.Element | ReactElement;
    /**
     * Returns a static or dynamic preview of the plugin.
     *
     * Called on the home page and in configuration selection dialogs to give users a preview
     * of what the plugin's main visualization looks like. This can use fake data, or display
     * a miniature/static version of the visualization, depending on the implementation.
     *
     * @returns A React element for preview display.
     */
    abstract getPreview(): JSX.Element | ReactElement;
    /**
     * Returns an array of supported file extensions for the plugin.
     *
     * The app uses this method to determine which plugins can be used to visualize a given capture file.
     *
     * @returns Array of supported file extension strings (e.g., ["mp4", "json"]).
     */
    abstract validExtensions(): string[];
    /**
     * Validate a capture plugin's output descriptor to decide if this playback
     * plugin can consume the produced artifact.
     *
     * Called by the host when evaluating which playback plugin(s) are compatible
     * with a capture configuration. Plugins can override this static method to
     * implement custom validation logic.
     *
     * @param descriptor - The descriptor object produced by the capture plugin or null if none.
     * @returns boolean - true if this playback plugin accepts the descriptor.
     */
    validateCaptureDescriptor(_descriptor: Record<string, unknown> | null): boolean;
}
//# sourceMappingURL=PlaybackPlugin.d.ts.map