/**
 * Describes the metadata required for a plugin to be loaded and managed by the PluginManager.
 * Every plugin must provide a metadata.json file matching this structure.
 *
 * @property id - Unique identifier for the plugin (within the publisher scope).
 * @property name - Name of the plugin.
 * @property description - Description of what the plugin does.
 * @property version - Plugin version (semver).
 * @property publisher - Information about the publisher: unique `id`, display `name`, and optional `url`.
 * @property repository - (Optional) Repository URL for the plugin source code.
 * @property author - (Optional) Author information, with `name` and `email`.
 * @property icon - Path(s) to plugin icons. May be a string (single icon) or an object with `light`/`dark` variants.
 * @property locales - (Optional) Path to a directory containing localization files for the plugin.
 * @property platform - (Optional) Supported platforms for the plugin. Values indicate compatibility (`true` for supported).
 * @property entryPoints - Entry points for loading the plugin in different contexts.
 *                        Keys correspond to application integration points (e.g. "mo.ui.renderer.plugin"),
 *                        values are string paths in the form "path/to/file.js#ExportName".
 * @property target - Defines whether the plugin is intended for the `"api"` backend or `"ui"` frontend.
 */
export interface PluginMetadata {
    id: string;
    name: string;
    description: string;
    version: string;
    publisher: {
        id: string;
        name: string;
        url?: string;
    };
    repository?: string;
    author?: {
        name?: string;
        email?: string;
    };
    icon?: {
        light?: string;
        dark?: string;
    } | string;
    locales?: string;
    platform?: {
        linux?: boolean;
        windows?: boolean;
        macos?: boolean;
    };
    entryPoints: {
        "mo.ui.renderer.plugin"?: string;
        "mo.ui.renderer.plugin.properties"?: string;
        [key: string]: string | undefined;
    };
    target: "api" | "ui";
}
//# sourceMappingURL=PluginMetadata.d.ts.map